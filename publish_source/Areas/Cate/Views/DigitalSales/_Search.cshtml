@using TSFramework.Libs.Processors
@model Core.Cate.Models.RM_DigitalSalesSearchModel

@{
    var excludedStatusIds = Model.ExcludedStatusIDs ?? new List<int>();
    var statusItems = Model.StatusItems ?? new List<Core.Cate.Models.RM_DigitalSalesStatusModel>();

    var checkedCount = statusItems.Count(x => !excludedStatusIds.Contains(x.StatusID));
    var totalCount = statusItems.Count;

    var defaultText = checkedCount == totalCount
        ? "Táº¥t cáº£"
        : (checkedCount == 0 ? "ChÆ°a chá»n tráº¡ng thÃ¡i" : $"{checkedCount} tráº¡ng thÃ¡i Ä‘Æ°á»£c chá»n");

    var applyYearLabel = AppProcessor.Messagor.GetMessage("DigitalSalesSearch_ApplyYear_Label");
    if (string.IsNullOrWhiteSpace(applyYearLabel) || applyYearLabel == "DigitalSalesSearch_ApplyYear_Label")
    {
        applyYearLabel = "NÄƒm Ã¡p dá»¥ng";
    }

    var statusLabel = AppProcessor.Messagor.GetMessage("DigitalSalesSearch_Status_Label");
    if (string.IsNullOrWhiteSpace(statusLabel) || statusLabel == "DigitalSalesSearch_Status_Label")
    {
        statusLabel = "Tráº¡ng thÃ¡i";
    }
}

<div class="Search search-opportunity card mb-3" id="SearchDigitalSales">
    <div class="card-header bgc-primary-d1">
        <div class="card-title text-white">
            <i class="fa fa-search"></i>&nbsp;@AppProcessor.Messagor.GetMessage("Label_Search")
        </div>
    </div>
    <div class="card-body p-2">
        @Html.HiddenFor(model => model.CustomerID, new { id = "CustomerID" })

        <!-- HÃ€NG 1: TÃŒM KIáº¾M, LOáº I HÃŒNH, TRáº NG THÃI, SWITCHES & NÄ‚M ÃP Dá»¤NG -->
        <div class="row align-items-end">
            <!-- 1. Tá»« khÃ³a -->
            <div class="col-xl-3 col-lg-3 col-md-6 col-12 mb-2 Search_Box keyWord">
                @Html.TitleFor(model => model.Keyword, new { @class = "font-bold text-sm-right" })
                @Html.TextBoxFor(model => model.Keyword, new { @class = "form-control", id = "Keyword", placeholder = AppProcessor.Messagor.GetMessage("DigitalSalesSearch_Keyword_Placeholder") })
            </div>

            <!-- 2. Loáº¡i hÃ¬nh -->
            <div class="col-xl-2 col-lg-2 col-md-3 col-sm-6 col-6 mb-2 Search_Box">
                @Html.TitleFor(model => model.BusinessType, new { @class = "font-bold text-sm-right" })
                <div>
                    <select id="BusinessType" name="BusinessType" class="form-control" onchange="filterStatusListByBusinessType(this.value);">
                        <option value="">@AppProcessor.Messagor.GetMessage("DigitalSalesSearch_BusinessType_AllOption")</option>
                        <option value="1" @(Model.BusinessType == 1 ? "selected" : "")>@AppProcessor.Messagor.GetMessage("DigitalSales_BusinessType_Opportunity")</option>
                        <option value="2" @(Model.BusinessType == 2 ? "selected" : "")>@AppProcessor.Messagor.GetMessage("DigitalSales_BusinessType_Project")</option>
                    </select>
                </div>
            </div>

            <!-- 3. Tráº¡ng thÃ¡i -->
            <div class="col-xl-2 col-lg-2 col-md-3 col-sm-6 col-6 mb-2 position-relative status-filter-wrapper Search_Box">
                <label class="font-bold text-sm-right status-label" for="statusDropdownButton">
                    @statusLabel
                </label>

                <!-- BUTTON -->
                <div id="statusDropdownButton"
                     class="form-control d-flex justify-content-between align-items-center cursor-pointer">
                    <span id="statusDropdownText" class="text-truncate">
                        @defaultText
                    </span>
                    <i class="fas fa-caret-down ml-2"></i>
                </div>

                <!-- DROPDOWN -->
                <div id="statusDropdownMenu"
                     class="border bg-white shadow-sm rounded p-2">
                    <!-- CHECK ALL -->
                    <div class="mb-2 border-bottom pb-2">
                        <label class="mb-0 w-100 cursor-pointer">
                            <input type="checkbox"
                                   id="checkAllStatus"
                                   @(checkedCount == totalCount ? "checked" : "") />
                            <span class="ml-1 font-weight-bold">
                                Táº¥t cáº£
                            </span>
                        </label>
                    </div>

                    <!-- ITEMS -->
                    @foreach (var item in statusItems)
                    {
                        var isChecked = !excludedStatusIds.Contains(item.StatusID);
                        var typePrefix = item.BusinessType == 1 ? "[CÆ¡ há»™i]" : "[Dá»± Ã¡n]";

                        <div class="mb-1 status-item-row" data-businesstype="@item.BusinessType">
                            <label class="mb-0 w-100 cursor-pointer text-nowrap">
                                <input type="checkbox"
                                       class="status-item"
                                       value="@item.StatusID"
                                       data-businesstype="@item.BusinessType"
                                       @(isChecked ? "checked" : "") />
                                <span class="ml-1 text-85">
                                    <span class="text-secondary opacity-75 font-bold mr-1">@typePrefix</span>@item.StatusName
                                </span>
                            </label>
                        </div>
                    }
                </div>
            </div>

            <!-- 4. NhÃ³m 2 Toggle: Trá»ng Ä‘iá»ƒm & Äang quan tÃ¢m -->
            <div class="col-xl-3 col-lg-3 col-md-8 col-12 mb-2 Search_Box search-switches-box">
                <label class="font-bold d-block text-sm-right d-none d-lg-block invisible" style="height: 18px; margin-bottom: 0.5rem;">&nbsp;</label>
                <div class="d-flex align-items-center flex-nowrap switches-wrapper" style="gap: 14px;">
                    <div class="custom-control custom-switch custom-switch-lg" title="Lá»c cÃ¡c dá»± Ã¡n trá»ng Ä‘iá»ƒm">
                        <input type="checkbox" class="custom-control-input cursor-pointer" id="chkFilterKeyProject">
                        <label class="custom-control-label font-bold text-dark cursor-pointer select-none mb-0 text-nowrap" for="chkFilterKeyProject" id="lblFilterKeyProject">
                            Trá»ng Ä‘iá»ƒm
                        </label>
                    </div>
                    <div class="custom-control custom-switch custom-switch-lg" title="Lá»c cÃ¡c cÆ¡ há»™i/dá»± Ã¡n báº¡n Ä‘ang quan tÃ¢m">
                        <input type="checkbox" class="custom-control-input cursor-pointer" id="chkFilterFollowed">
                        <label class="custom-control-label font-bold text-dark cursor-pointer select-none mb-0 text-nowrap" for="chkFilterFollowed" id="lblFilterFollowed">
                            Äang quan tÃ¢m
                        </label>
                    </div>
                </div>
            </div>

            <!-- 5. NÄƒm Ã¡p dá»¥ng -->
            <div class="col-xl-2 col-lg-2 col-md-4 col-12 mb-2 Search_Box">
                <label class="font-bold text-sm-right" for="ApplyYear">@applyYearLabel</label>
                <div>
                    <select id="ApplyYear" name="ApplyYear" class="form-control" onchange="reloadSalesTable();">
                        <option value="">-- Táº¥t cáº£ --</option>
                        @for (int y = DateTime.Now.Year + 3; y >= DateTime.Now.Year - 5; y--)
                        {
                            <option value="@y" @((Model.ApplyYear.HasValue ? Model.ApplyYear.Value : DateTime.Now.Year) == y ? "selected" : "")>@y</option>
                        }
                    </select>
                </div>
            </div>
        </div>

        <!-- HÃ€NG 2: PHÃ’NG BAN, NHÃ‚N VIÃŠN, THá»œI GIAN & Cá»¤M NÃšT Báº¤M -->
        <div class="row align-items-end">
            <!-- 6. PhÃ²ng ban -->
            <div class="col-xl-3 col-lg-3 col-md-6 col-12 mb-2 Search_Box Dropdown">
                @Html.TitleFor(model => model.DepartmentID, new { @class = "font-bold" })
                @Html.DropDownListFor(model => model.DepartmentID, Model.ListDepartment, AppProcessor.Messagor.GetMessage("DigitalSalesSearch_Department_Option"), new { @class = "form-control", id = "DepartmentID" })
            </div>

            <!-- 7. NhÃ¢n viÃªn -->
            <div class="col-xl-2 col-lg-2 col-md-6 col-12 mb-2 Search_Box Dropdown">
                @Html.TitleFor(model => model.EmployeeID, new { @class = "font-bold" })
                @Html.DropDownListFor(model => model.EmployeeID, Model.ListEmployee, AppProcessor.Messagor.GetMessage("DigitalSalesSearch_Employee_Option"), new { @class = "form-control", id = "EmployeeID" })
            </div>

            <!-- 8. Tá»« ngÃ y -->
            <div class="col-xl-2 col-lg-2 col-md-3 col-sm-6 col-6 mb-2 Search_Box">
                @Html.TitleFor(model => model.FromDate, new { @class = "font-bold text-sm-right" })
                <div class="input-group date w-100" id="dpFromDate">
                    @Html.TextBoxFor(model => model.FromDate, new { @class = "form-control", id = "FromDate", placeholder = AppProcessor.Messagor.GetMessage("DigitalSalesSearch_Date_Placeholder"), autocomplete = "off" })
                    <div class="input-group-append">
                        <span class="input-group-text"><i class="fa fa-calendar"></i></span>
                    </div>
                </div>
            </div>

            <!-- 9. Äáº¿n ngÃ y -->
            <div class="col-xl-2 col-lg-2 col-md-3 col-sm-6 col-6 mb-2 Search_Box">
                @Html.TitleFor(model => model.ToDate, new { @class = "font-bold text-sm-right" })
                <div class="input-group date w-100" id="dpToDate">
                    @Html.TextBoxFor(model => model.ToDate, new { @class = "form-control", id = "ToDate", placeholder = AppProcessor.Messagor.GetMessage("DigitalSalesSearch_Date_Placeholder"), autocomplete = "off" })
                    <div class="input-group-append">
                        <span class="input-group-text"><i class="fa fa-calendar"></i></span>
                    </div>
                </div>
            </div>

            <!-- 10. Cá»¥m nÃºt báº¥m thao tÃ¡c -->
            <div class="col-xl-3 col-lg-3 col-md-6 col-12 mb-2 search-action-col">
                <label class="font-bold d-block text-sm-right d-none d-lg-block invisible" style="height: 18px; margin-bottom: 0.5rem;">&nbsp;</label>
                <div class="search-action-buttons">
                    <button type="button" class="btn btn-primary text-nowrap" onclick="reloadSalesTable();">
                        <i class="fa fa-search-plus mr-1"></i>@AppProcessor.Messagor.GetMessage("Label_Search")
                    </button>
                    <button type="button" class="btn btn-default btn-outline-secondary text-nowrap" onclick="resetSalesSearch();">
                        <i class="fa fa-undo mr-1"></i>Äáº·t láº¡i
                    </button>
                    <button type="button" class="btn btn-warning font-bold text-nowrap shadow-sm" id="btnExportDigitalSales" onclick="exportDigitalSales();" title="Táº£i danh sÃ¡ch theo Ä‘iá»u kiá»‡n tÃ¬m kiáº¿m">
                        <i class="fa fa-download mr-1"></i>Táº£i danh sÃ¡ch
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    window._excludedStatusIds = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(excludedStatusIds));

    $(document).ready(function () {
        if (typeof updateStatusDropdownText === 'function') {
            updateStatusDropdownText();
        }

        if ($.fn.datepicker) {
            $('#dpFromDate').datepicker({
                format: 'dd/mm/yyyy',
                autoclose: true,
                todayHighlight: true,
                language: 'vi'
            });
            $('#dpToDate').datepicker({
                format: 'dd/mm/yyyy',
                autoclose: true,
                todayHighlight: true,
                language: 'vi'
            });
        }

        $('#SearchDigitalSales').on('keydown', 'input, select', function (e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                if (typeof reloadSalesTable === 'function') {
                    reloadSalesTable();
                }
            }
        });

        $('#SearchDigitalSales').on('change', 'select:not(#DepartmentID):not(#BusinessType)', function () {
            if (typeof reloadSalesTable === 'function') {
                reloadSalesTable();
            }
        });

        // Lá»c danh sÃ¡ch checkbox tráº¡ng thÃ¡i theo Loáº¡i hÃ¬nh (CÆ¡ há»™i / Dá»± Ã¡n)
        $('#BusinessType').change(function () {
            var businessType = $(this).val();
            if (typeof filterStatusListByBusinessType === 'function') {
                filterStatusListByBusinessType(businessType);
            }
        });

        // Load danh sÃ¡ch nhÃ¢n sá»± phá»¥ trÃ¡ch (AM) theo phÃ²ng ban thuá»™c TTKDGP
        $('#DepartmentID').change(function () {
            var departmentId = $(this).val();
            var $employee = $('#EmployeeID');

            $employee.empty().append('<option value="">-- Chá»n nhÃ¢n viÃªn --</option>');

            $.get('/Cate/DigitalSales/GetEmployeesByDepartment', {
                departmentId: departmentId ? departmentId : 0
            }, function (data) {
                if (data && data.length > 0) {
                    $.each(data, function (i, item) {
                        $employee.append(
                            $('<option>').val(item.Value).text(item.Text)
                        );
                    });
                }
                $employee.trigger("chosen:updated");
                if ($.fn.select2) {
                    $employee.trigger("change.select2");
                }
                if (typeof reloadSalesTable === 'function') {
                    reloadSalesTable();
                }
            });
        });
    });
</script>

<link rel="stylesheet" href="~/Areas/Cate/Views/DigitalSales/_Search.css?v=@DateTime.Now.Ticks" />