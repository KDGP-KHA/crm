@using TSFramework.Libs.Processors
@{
    ViewBag.Title = AppProcessor.Messagor.GetMessage("SharedDoc_Modal_Title_Add");
    Layout = "~/Views/Shared/_Form.cshtml";
}

@section FormType {
    success
}

@section FormIcon {
    <i class="fa fa-upload"></i>
}

@section FormBody {
    @using (Html.BeginForm("Add", "SharedDocument", new { area = "Sys" }, FormMethod.Post,
        new { encType = "multipart/form-data", id = "AddSharedDocument", @class = "form-horizontal" }))
    {
        <div id="bodyForm" class="bodyForm">
            @Html.Partial("_AddDoc_View")
        </div>
    }

    <script type="text/javascript">
        (function () {
            var $form = $('#AddSharedDocument'), $modal = $form.closest('.modal');
            $modal.find('#btnSave').off('click.sharedDoc').on('click.sharedDoc', function (e) {
                e.preventDefault();
                $form.submit();
            });
            $form.ajaxForm({
                beforeSubmit: function () {
                    if ($form.valid && !$form.valid()) return false;
                    $modal.find('#btnSave').prop('disabled', true);
                    return true;
                },
                success: function (r) {
                    $modal.find('#btnSave').prop('disabled', false);
                    SharedDocument_OnProcessSuccess(r, 'AddSharedDocument');
                },
                error: function () {
                    $modal.find('#btnSave').prop('disabled', false);
                    toastr.error('@Html.Raw(HttpUtility.JavaScriptStringEncode(AppProcessor.Messagor.GetMessage("Common_Message_Error")))');
                }
            });
        })();
    </script>
}